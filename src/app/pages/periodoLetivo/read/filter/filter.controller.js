export default class PeriodoLetivoReadFilterController {
  /*constructor($scope) {
    'ngInject'
    this.$scope = $scope

    this.filterKeyOptions = [
      {key: '', label: ''},
      {key: 'nome', label: ''}
    ]
    this.filterKeySelected = 'none'
    this.filterQuery = ''
    this.$scope.$watch(() => this.filterKeySelected, this.filterPeriodoLetivos())
    this.$scope.$watch(() => this.filterQuery, this.filterPeriodoLetivos());
  }

  filterPeriodoLetivos () {
    return (key) => {
      this.parent.periodoLetivo = this.parent.permPeriodoLetivos
        .filter((periodoLetivo) =>
          new RegExp(this.filterQuery, 'gi')
            .test(periodoLetivo[this.filterKeySelected])
        )
    }
  }

  clearFilters() {
    this.filterKeySelected = 'none'
    this.filterQuery = ''
  }*/
}
